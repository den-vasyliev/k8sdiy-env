apiVersion: v1
kind: Service
metadata:
  name: {{ include "ed210.fullname" . }}
  labels:
    {{- include "ed210.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "ed210.selectorLabels" . | nindent 4 }}
